$schema: "https://moonrepo.dev/schemas/project.json"

layer: "application"
stack: "frontend"
language: "typescript"

tasks:
  dev:
    command: "vite"
    preset: "server"
  build:
    command: "vite build"
    outputs:
      - "dist"
  typecheck:
    command: "tsc --noEmit"
