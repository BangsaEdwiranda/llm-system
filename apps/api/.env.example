DATABASE_URL=sqlite:///./speechify.db

# Secret used to sign auth tokens. Generate a real random value for any
# non-local environment, e.g.: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=please-generate-a-real-secret
